apiVersion: v2
name: runai
description: Run:ai Control Plane — app of apps deploying cert-manager, ingress-nginx, Prometheus, and the Run:ai control plane in dependency order
type: application
version: 5.0.0
