cpURL: ""
adminUsername: "admin@run.ai"
adminPassword: ""
clusterName: ""
clusterURL: ""
agentVersion: ""
credentialsNamespace: "argocd"
image: "alpine/k8s:1.31.4"

# CA certificate for TLS verification.
# Set caSecretName to mount a Secret containing the CA cert (key configured via caKey).
# Works for both customCAName and selfSignedCA (trust-manager distributes as a Secret).
caSecretName: ""
caKey: "runai-ca.pem"
