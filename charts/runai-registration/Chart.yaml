apiVersion: v2
name: runai-registration
description: Registers a workload cluster with the Run:ai Control Plane and writes credentials to a Secret for consumption by the runai-cluster agent chart.
type: application
version: 1.7.0
